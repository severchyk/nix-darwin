{ ... }:

{
  programs.micro = {
    enable = true;

    settings = {
      colorscheme = "simple";
      matchbracestyle = "highlight";
      savecursor = true;
      saveundo = true;
      scrollbar = true;
      filemanager-openonstart = true;
    };
  };
}
